/*
 * Created on 2025-03-20 ( 10:03:15 )
 * Generated by Telosys ( http://www.telosys.org/ ) version 4.2.0
 */
package org.demo.entities;

import static junit.framework.TestCase.assertNotNull;

import org.demo.entities.tooling.JpaTest;
import org.junit.Test;

public class PhoneNumbersCustomersJpaTest extends JpaTest {

    public static PhoneNumbersCustomers getInitEntity() {
    	PhoneNumbersCustomers entity = new PhoneNumbersCustomers();
		entity.setPhoneNumber( "AAAAAAAAAAAAAAAAAAAA" ) ;
		entity.setCustomerId( CustomerJpaTest.getInitEntity().getCustomerId() ) ;
    	return entity;
	}
	
    private PhoneNumbersCustomers createEntity() {
    	PhoneNumbersCustomers entity = new PhoneNumbersCustomers();
		entity.setPhoneNumber( "BBBBBBBBBBBBBBBBBBBB" ) ;
		entity.setCustomerId( CustomerJpaTest.getInitEntity().getCustomerId() ) ;
    	return entity;
    }


    private Object getEntityKey(PhoneNumbersCustomers entity) {
    	return entity.getPhoneNumber();
    }

    @Test
    public void testFind() {
    	PhoneNumbersCustomers e = find(PhoneNumbersCustomers.class, getEntityKey(getInitEntity()) );
		assertNotNull(e);
    }
    
    @Test
    public void testPersistFind() {
    	PhoneNumbersCustomers entity = createEntity();
    	Object key = getEntityKey(entity);

    	// if entity doesn't exist yet
    	if ( find(PhoneNumbersCustomers.class, key) == null ) {

	    	// Create with 'persist'
    		persistAndRefresh(entity);
    	
			// Find
			PhoneNumbersCustomers entity2 = find(PhoneNumbersCustomers.class, key);
			assertNotNull(entity2);
        
			// Remove (use only if no risk of referential integrity constraint violation)
			// findAndRemove(PhoneNumbersCustomers.class, key);			
			// assertNull(find(PhoneNumbersCustomers.class, key));
		}
    }

    @Test
    public void testMergeFind() {
    	PhoneNumbersCustomers entity = createEntity();
    	Object key = getEntityKey(entity);

    	// Create or update with 'merge'
    	mergeAndRefresh(entity);
    	
    	// Find
    	PhoneNumbersCustomers entity2 = find(PhoneNumbersCustomers.class, key);
        assertNotNull(entity2);
        
		// Remove (use only if no risk of referential integrity constraint violation)
		// findAndRemove(PhoneNumbersCustomers.class, key);			
        // assertNull(find(PhoneNumbersCustomers.class, key));
    }

}
